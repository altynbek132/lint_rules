include: package:flutter_lints/flutter.yaml

# * // ignore: name_of_lint
# * // ignore_for_file: name_of_lint

analyzer:
  plugins:
    - ac_code_metrics

linter:
  # true - active
  # false - inactive

  rules:
